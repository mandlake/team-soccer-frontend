import API_URL from './dotenv';

export const API_PLAYERS = `${API_URL()}/players`;
export const API_SCHEDULES = `${API_URL()}/schedules`;
export const API_STADIUMS = `${API_URL()}/stadiums`;
export const API_TEAMS = `${API_URL()}/teams`;
